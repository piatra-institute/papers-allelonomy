[project]
name = "allelonomy-sim"
version = "0.1.0"
description = "Three mechanisms of the recursive formation loop: stratification without a planner, the exit-voice decay spiral, and epistemic regimes under honest and captured institutions"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
